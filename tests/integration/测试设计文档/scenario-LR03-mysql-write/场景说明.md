# LR03 MySQL 写入场景（scenario-LR03-mysql-write）

## 场景概述

验证 log-reader 的 `mod_log_mysql` 模块：读取 protobuf 访问日志，将 AI 请求日志按固定 89 列映射写入 MySQL 明细表 `bfe_ai_request_log`，并保证重发/补读幂等。

与 LR01（Kafka 输出）不同，本场景不依赖 MockKafka，而是向真实 MySQL 写入并直接查询数据库断言行内容。

## 被测进程与配置

- **被测进程**：真实 `log-reader` 二进制，`Modules = mod_log_mysql`，`-b` 从文件头读取。
- **日志来源**：测试代码直接生成 b2log 格式 protobuf 日志（`common.LogGenerator`），与 LR01 相同。
- **MySQL 来源**（`common.MysqlEnv` 按优先级选择）：
  1. 环境变量 `LR_MYSQL_DSN`（格式 `user:pass@tcp(host:port)/dbname`，凭据需具备 CREATE/DROP DATABASE 权限）；测试创建专用随机名数据库（如 `bfe_report_lr03_<ts>`），结束后 DROP，**绝不使用 DSN 中自带的数据库名**；
  2. testcontainers 启动 `mysql:8.0` 容器（需本机 Docker 可用）；
  3. 都不可用则 `t.Skip`。
- **表结构**：testdata 内嵌 DDL 副本 `testdata/ddl/bfe_ai_request_log.sql` 初始化（副本说明见文件头注释；权威 DDL 归 ai-gateway-api 仓库发布后以其为准重新生成）。
- **写入配置**（`testdata/mod_log_mysql/mod_log_mysql.conf`）：`BatchSize = 5`、`FlushIntervalMs = 200`、`MaxRetries = 3`，读侧 `MaxSizePerBatch = 10`（config.conf），刻意小于用例日志量以覆盖攒批/拆分路径。
- **监控端口**：8993（避免与 LR01 的 8992 冲突，包间可并行运行）。

## 测试用例

| 用例 | 文件 | 说明 |
|------|------|------|
| TC-01 基本写入全字段校验 | `lr03_mysql_write_test.go` `TestLR03_BasicWrite` | 3 条不同模型日志（唯一键互不冲突）→ 断言行数与逐列取值：标量列、字符串零值→NULL（err_code）、布尔→TINYINT、JSON 列内容、标签打平（level1 有值/level3 为 NULL）、log_time 与 timestamp 的对应 |
| TC-02 幂等重放 | `TestLR03_IdempotentReplay` | 同一唯一键日志改值重发 → 行数不变、值被覆盖；再重启 log-reader（-b 全量补读，文件内两条同键记录）→ 仍只有 1 行、最终值为最后一条 |
| TC-03 零值规则 | `TestLR03_ZeroValueToNull` | proto 必填字段置语义零值的日志 → 字符串零值列（err_code/err_msg/proto/header_host/origin_uri）为 NULL，空结构（req_headers/ai_auth_hit_quota_plans/level1）为 NULL，布尔/数值零值（is_trust_src_ip/ai_stream/ai_input_tokens）写 0 而非 NULL |
| TC-04 批次拆分 | `TestLR03_BatchSplit` | 25 条日志 > MaxSizePerBatch(10) > BatchSize(5) → 覆盖读侧拆分与写侧多批 flush；断言 25 行齐全且 logid→model 对应正确 |

## 关键断言点

- **列映射正确性**：以 `reader_modules/mod_log_mysql/field_mapper.go` 的 89 列清单为基准，抽样覆盖全部列类别（标量/布尔/JSON/标签打平/log_time）。
- **零值规则**：字符串零值→NULL；数值/布尔零值→原值（0）。
- **幂等**：`INSERT ... ON DUPLICATE KEY UPDATE` 行为 = 行数不增、值覆盖，覆盖"重发"与"-b 补读"两种重放形态。
- **时间列**：`log_time` = pb `timestamp`（Unix 秒），以 UTC 比较。

## 环境要求与运行方式

```bash
# 方式一：外部实例（本地开发常用）
LR_MYSQL_DSN="root:****@tcp(127.0.0.1:3306)/" \
  go test ./tests/integration/implementation/scenario-LR03-mysql-write/ -v

# 方式二：testcontainers（CI，需 Docker）
go test ./tests/integration/implementation/scenario-LR03-mysql-write/ -v

# 单用例
go test ./tests/integration/implementation/scenario-LR03-mysql-write/ \
  -run TestLR03_IdempotentReplay -v
```

无 DSN 且无 Docker 时全部用例 `t.Skip`，不影响其他场景。

## 实现期间发现并修复的问题

1. **reader_conf 模块白名单遗漏**：`reader_conf/conf_load.go` 的 `modulesSupport` 未加入 `mod_log_mysql`，导致配置加载报 `module[mod_log_mysql] is not support`（单测/编译均无法发现，集成测试发现后已修复）。
2. **参考 DDL 行大小超限**：设计参考 DDL 中 8 个长文本列（origin_uri/final_uri/x_forward_for/authorization/referrer/user_agent/cookie/res_location）使用 VARCHAR，utf8mb4 下总长度超 MySQL 65535 字节行上限（Error 1118），测试副本已改为 TEXT，权威 DDL 需同步。
3. **参考 DDL 幂等键与零值规则冲突**：`ai_apikey_id`/`ai_requested_model` 的 `NOT NULL` 使未认证请求（apikey 为空→NULL）整批写入失败（Error 1048），测试副本已将这两列改为可空；`hostid`/`log_time` 由 log-reader 构造保证非空，保持 NOT NULL。
